// project import
// import dashboard from './dashboard';
import { NavItemType } from '@/types/menu';
import { admin } from './admin';
// ==============================|| MENU ITEMS ||============================== //



const navItems: { items: NavItemType[] } = {
  items: [
		admin,
  ]

};

export default navItems;
